import java.text.ParseException;
import javax.swing.*;
import java.awt.*;

class Main {
   public static void main (String[] args)throws ParseException {
      DemoMinimale demoMinimale = new DemoMinimale();
      demoMinimale.menuPrincipal();

      // Test test = new Test();
      // test.test_unitaires();

      // Init init = new Init();
      // init.remplirfromBDD();
      // Menunormal frame = new Menunormal(init);
      // frame.setLayout(new GridLayout(0,1));
      // frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
      // frame.pack();
      // frame.setSize(450, 900);
      // frame.setVisible(true);

   }
}
